<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Merge_Blocked_Date__c</fullName>
    <description>Date the account has been deemed valid regardless of deduplication rules and should not be included as the duplicate in any deduplication grouping.</description>
    <inlineHelpText>Date the account has been deemed valid regardless of deduplication rules and should not be included as the duplicate in any deduplication grouping.</inlineHelpText>
    <label>Merge Blocked Date</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
